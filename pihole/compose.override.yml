# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# pihole
# 


services:
  pihole:
    environment:
      # From the not git versioned global.env
      - PUID=${APPUSER_UID}
      - PIHOLE_UID=${APPUSER_UID}
      - PGID=${APPUSER_GID}
      - PIHOLE_GID=${APPUSER_GID}
      - TZ=${MY_TZ}
      - FTLCONF_webserver_api_password='correct horse battery staple'
    cap_add:
      #  we need to be able to change ownership of log files and databases in case FTL is started as a different user than pihole
      - CHOWN
    restart: unless-stopped