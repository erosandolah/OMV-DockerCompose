# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# adguardhome
# 

services:
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: adguardhome
    restart: unless-stopped
    # Volumes keep your blocklists, custom rules, and login credentials safe when updating
    volumes:
      - ${DC_DOCKERDATA}/adguardhome/work:/opt/adguardhome/work
      - ${DC_DOCKERDATA}/adguardhome/conf:/opt/adguardhome/conf
    ports:
      # Plain DNS (Required for your NetBird client queries)
      - "53:53/tcp"
      - "53:53/udp"
      # Web UI Admin Dashboard (Changed to 8080 to avoid OMV web interface conflicts)
      - "8081:80/tcp"
      # Initial Setup Wizard Port (Only used the very first time you configure it)
      - "3000:3000/tcp"
      # Optional: Encrypted DNS ports (Uncomment if you want to use them later)
      #- "784:784/udp"   # DNS-over-QUIC
      #- "853:853/tcp"   # DNS-over-TLS
      #- "853:853/udp"   # DNS-over-QUIC
    network_mode: bridge
