# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# docmost
# 


services:
  docmost:
    environment:
      # From the not git versioned global.env
      - PUID=${APPUSER_UID}
      - PGID=${APPUSER_GID}
      - TZ=${MY_TZ}
      - APP_SECRET=${DOCMOST_APP_SECRET}
      - DATABASE_URL=postgresql://docmost:${DOCMOST_POSTGRES_PASSWORD}@db:5432/docmost
  db:
    environment:
      # From the not git versioned global.env
      - PUID=${APPUSER_UID}
      - PGID=${APPUSER_GID}
      - TZ=${MY_TZ}
      - POSTGRES_PASSWORD=${DOCMOST_POSTGRES_PASSWORD}
  redis:
    environment:
      # From the not git versioned global.env
      - PUID=${APPUSER_UID}
      - PGID=${APPUSER_GID}
      - TZ=${MY_TZ}